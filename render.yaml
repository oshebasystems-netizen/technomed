services:
  - type: web
    name: technomed-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: technomed-db
          property: connectionString

databases:
  - name: technomed-db
    plan: free
